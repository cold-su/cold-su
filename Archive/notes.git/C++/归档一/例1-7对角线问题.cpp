#include <bits/stdc++.h>
#include <cmath>
using namespace std;

int main(int argc, char const *argv[])
{
	cout << sqrt(pow(6,2) + pow(9,2));
	// sqrt() //是计算括号里的平方根，返回浮点数
	// pow(6,2) //意思是计算6的2次方
	return 0;
}