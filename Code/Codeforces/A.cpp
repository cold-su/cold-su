#include <bits/stdc++.h>
using namespace std;
void solve() {
    int n;
    std::cin >> n;

    int i=0;
    vector<int> v;
    while (n--) {
        std::cin >> v[i++];
    }
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int t;
    std::cin >> t;
    while (t--) solve();
    return 0;
}