#include <iostream>
#include <algorithm>
#include <vector>
#include <cstdio>
#include <cmath>
#include <cstring>

void read(std::vector<int> array){
    for(auto prev:array){
        std::cout<<prev<<" ";
    }
    std::cout<<"\n";
}

void solve(){
    
}

int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);
    
    return 0;
}